# Подключение / Wiring

## Назначение GPIO

| Функция | GPIO | Примечание |
|---|---:|---|
| Диммер помпы, gate | GPIO6 | RobotDyn/Robotron AC dimmer, `ac_cycle_skip` |
| Диммер помпы, zero-cross | GPIO7 | RobotDyn/Robotron AC dimmer, `ac_cycle_skip` |
| MAX31865 SDI/MOSI | GPIO8 | PT100, SPI |
| SSR нагревателя | GPIO13 | `slow_pwm`, период 1 с |
| Реле помпы / разрешение бойлера | GPIO15 | Роль зависит от `use_pump_dimmer` |
| MAX31865 SDO/MISO | GPIO16 | PT100, SPI |
| MAX31865 CS | GPIO17 | PT100, SPI chip select |
| MAX31865 SCLK | GPIO18 | PT100, SPI clock |
| Реле brew-клапана | GPIO41 | Активный низкий уровень |
| Силовое реле машины | нет | Сейчас это логический switch без физического GPIO |
| Nextion TX | GPIO1 | Закомментированная заготовка |
| Nextion RX | GPIO2 | Закомментированная заготовка |
| HX711 DOUT | GPIO38 | Заготовка под весы |
| HX711 CLK | GPIO48 | Заготовка под весы |
| XDB401 I2C SDA | GPIO11 | Заготовка под датчик давления |
| XDB401 I2C SCL | GPIO12 | Заготовка под датчик давления |
| Кнопка Power | GPIO4 | Вход с внутренней подтяжкой |
| Кнопка Brew/пролива | GPIO21 | Вход с внутренней подтяжкой |
| Кнопка подачи воды | GPIO10 | Вход с внутренней подтяжкой |
| Кнопка пара | GPIO9 | Вход с внутренней подтяжкой |
| Статусный LED | GPIO47 | Цифровой выход |
| Датчик уровня воды | GPIO5 | XKC-Y25-NPN, open-collector, активный LOW |

## PT100 и MAX31865

Конфигурация рассчитана на трёхпроводной PT100, опорный резистор 430 Ом и сетевой фильтр 50 Гц. Проверьте перемычки и фактический опорный резистор своей платы MAX31865.

В новой распиновке GPIO11/GPIO12 зарезервированы под XDB401 I2C, а GPIO13 - под SSR нагревателя. MAX31865 перенесён на SPI: SDO/MISO GPIO16, CS GPIO17, SCLK GPIO18, SDI/MOSI GPIO8.

## Датчик воды XKC-Y25-NPN

GPIO ESP32-S3 рассчитаны на 3,3 В и не являются устойчивыми к 5 В. XKC-Y25-NPN использует open-collector, поэтому сигнальный выход подтягивается внутренним резистором ESP32 к 3,3 В.

- коричневый: питание датчика +5 В;
- синий: GND датчика и общий GND с ESP32;
- жёлтый: сигнальный выход на GPIO5;
- чёрный: выбор логики выхода, подключается согласно маркировке конкретной ревизии датчика.

Не устанавливайте внешнюю подтяжку жёлтого провода к 5 или 12 В. Конфигурация использует `pullup: true` и `inverted: true`, поэтому состояние `ON` означает наличие воды.

## Низковольтные кнопки

Кнопки `Brew Shot`, `Hot Water` и `Steam Mode` подключаются сухим контактом между соответствующим GPIO и `GND` ESP. Конфигурация использует внутреннюю подтяжку (`pullup: true`) и активный низкий уровень (`inverted: true`).

Не подавайте на эти входы 5 В, 3,3 В, сетевой ноль, фазу или защитную землю. Это только замыкание `GPIO` на низковольтный `GND` ESP.

`Brew Shot` запускает автоматический пролив по текущему профилю. `Hot Water` включает помпу без brew-клапана. `Steam Mode` переключает PID в режим пара и возвращает режим приготовления при отпускании/выключении.

## Реле помпы, brew-клапана и power

Помпа и brew-клапан заведены как отдельные GPIO-реле с `restore_mode: ALWAYS_OFF`. При старте ESP, выключении общего реле машины и программном перегреве оба выхода принудительно выключаются.

GPIO15 используется либо для помпы, либо как разрешение бойлера, GPIO41 используется для brew-клапана. В конфигурации оба релейных выхода активны низким уровнем (`inverted: true`), как у большинства релейных модулей. Если конкретный модуль включается высоким уровнем, уберите `inverted: true` для этих выходов.

Режим выбирается в `substitutions` через `use_pump_dimmer`:

- `false`: GPIO15 работает как реле помпы. Логический `Silvia Power Relay` не имеет физического GPIO, а нагрев разрешается/запрещается через SSR.
- `true`: GPIO15 не используется как реле помпы и становится разрешением бойлера. Помпа должна управляться диммером GPIO6/GPIO7.

При `use_pump_dimmer: "true"` помпа управляется через локальный ESPHome external component `ac_cycle_skip` на GPIO6/GPIO7. Это не phase-cut dimmer: компонент пропускает или блокирует полные периоды сети, синхронизируясь по zero-cross. В Home Assistant доступны `Silvia Manual Pump Power`, `Silvia Pump Ramp Time`, `Silvia Pump Start Boost` и `Silvia Pump Start Boost Time`.

Без датчика давления это open-loop управление: процент мощности не является давлением в барах. Перед первыми тестами проверяйте работу на малых процентах без кофе и под постоянным наблюдением.

`Silvia Power Relay` - историческое имя HA-entity. В двухрелейной схеме это логический switch без физического GPIO: он разрешает работу автоматики и нагрева через SSR, но не щёлкает отдельным реле питания.

## Mains wiring

The mains and low-voltage sections must be physically separated. Use correctly rated relays, SSRs, terminals, wire, insulation, fusing, and protective earth. The exact mains wiring is intentionally not documented until the assembled machine has been verified.
